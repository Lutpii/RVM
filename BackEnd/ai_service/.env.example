# Required — no default. Must match BackEnd/.env's AI_SERVICE_KEY exactly.
AI_API_KEY=

# Comma-separated origins allowed to call this service from a browser.
ALLOWED_ORIGINS=https://localhost:5173,https://localhost:4173
